name: Call Shared Ansible Lint Workflow

on: [push]

permissions:
  contents: write

jobs:
  call-shared-workflow:
    uses: AdrienMS-Cap/tst-shared-pipeline/.github/workflows/create_workflow@main
    with:
      repository: ${{ github.repository }}
      branch: ${{ github.ref_name }} 